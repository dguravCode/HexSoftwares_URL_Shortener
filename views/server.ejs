<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL-Shortener</title>
  <style>
    body {
      font-family: sans-serif;
      background-color: #121212;
      color: #e0e0e0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .container {
      background-color: #1e1e1e;
      padding: 2rem;
      border: 1px solid #333;
      border-radius: 8px;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 0 15px rgba(0, 255, 157, 0.1);
    }

    h1 {
      text-align: center;
      color: #00ff9d;
      /* Neon Green */
      margin-top: 0;
    }

    label {
      display: block;
      margin-bottom: 8px;
      color: #aaa;
    }

    input {
      width: 100%;
      padding: 10px;
      background-color: #2c2c2c;
      border: 1px solid #444;
      color: #fff;
      border-radius: 4px;
      box-sizing: border-box;
      margin-bottom: 15px;
    }

    input:focus {
      outline: none;
      border-color: #00ff9d;
    }

    button {
      width: 100%;
      padding: 10px;
      background-color: #00ff9d;
      color: #121212;
      border: none;
      font-weight: bold;
      cursor: pointer;
      border-radius: 4px;
    }

    button:hover {
      background-color: #00cc7d;
    }

    .short-url {
      color: #aaa;
      text-decoration: none;
    }

    .short-url:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <main>
    <div class="container">
      <h1>URL Shortener</h1>
      <form action="/Shorten" , method="post"> <!-- method=POST means sending -->
        <label for="longUrl">Enter Full URL Path:</label>
        <input type="text" id="longUrl" name="longUrl" required>
        <button type="submit">Shorten</button>
      </form>

      <% if(shortUrl) { %>
        <p>
          Short URL:
          <a class="short-url" href="<%= shortUrl %>" target="_blank">
            <%= shortUrl %>
          </a>
        </p>
      <% } %>
      
    </div>
  </main>
</body>

</html>